<?php

namespace {{dir_namespace}}\Features\PostTypes;

class {{post_type_class}}PostType
{
  public static $slug = '{{post_type}}';
  /**
   * Enregistrement du type de contenu recipe
   *
   * @return void
   */
  public static function register()
  {
    // label pour le type de contenu
    $labels = array(
      'name'                     => __('{{post_type_class}}'),
      'singular_name'            => __('{{post_type_class}}'),
      'add_new'                  => __('Add new {{post_type_class}}'),
      'add_new_item'             => __('add new item {{post_type_class}}'),
      'edit_item'                => __('Edit {{post_type_class}}'),
      'new_item'                 => __('New {{post_type_class}}'),
      'view_item'                => __('View {{post_type_class}}'),
      'view_items'               => __('View {{post_type_class}}'),
      'search_items'             => __('Search {{post_type_class}}'),
      'not_found'                => __('No {{post_type_class}} found.'),
      'not_found_in_trash'       => __('No {{post_type_class}} found in Trash.'),
      'parent_item_colon'        => array(null, __('Parent Page:')),
      'all_items'                => __('All {{post_type_class}}'),
      'archives'                 => __('{{post_type_class}}'),
      'attributes'               => __('{{post_type_class}} Attributes'),
      'insert_into_item'         => __('Insert into {{post_type_class}}'),
      'uploaded_to_this_item'    => __('Uploaded to this {{post_type_class}}'),
      'featured_image'           => _x('Featured Image', '{{post_type_class}}'),
      'set_featured_image'       => _x('Set featured image', '{{post_type_class}}'),
      'remove_featured_image'    => _x('Remove featured image', '{{post_type_class}}'),
      'use_featured_image'       => _x('Use as featured image', '{{post_type_class}}'),
      'filter_items_list'        => __('Filter {{post_type_class}} list'),
      'items_list_navigation'    => __('{{post_type_class}} list navigation'),
      'items_list'               => __('{{post_type_class}} list'),
      'item_published'           => __('{{post_type_class}} published.'),
      'item_published_privately' => __('{{post_type_class}} published privately.'),
      'item_reverted_to_draft'   => __('{{post_type_class}} reverted to draft.'),
      'item_scheduled'           => __('{{post_type_class}} scheduled.'),
      'item_updated'             => __('{{post_type_class}} updated.'),

    );

    $options = array(
      'labels'                => $labels,
      // 'description'           => '',
      'public'                => true,
      // 'hierarchical'          => false,
      // 'exclude_from_search'   => null,
      // 'publicly_queryable'    => null,
      // 'show_ui'               => null,
      // 'show_in_menu'          => null,
      // 'show_in_nav_menus'     => null,
      // 'show_in_admin_bar'     => null,
      // 'menu_position'         => null,
      // 'menu_icon'             => null,
      // 'capability_type'       => 'post',
      // 'capabilities'          => array(),
      // 'map_meta_cap'          => null,
      'supports'              => array('title', 'editor', 'excerpt', 'thumbnail'),
      // 'register_meta_box_cb'  => null,
      // 'taxonomies'            => array(),
      // 'has_archive'           => false,
      // 'rewrite'               => true,
      // 'query_var'             => true,
      // 'can_export'            => true,
      // 'delete_with_user'      => null,
      // 'show_in_rest'          => false,
      // 'rest_base'             => false,
      // 'rest_controller_class' => false,
      // '_builtin'              => false,
      // '_edit_link'            => 'post.php?post=%d',
    );

    register_post_type(
      self::$slug,
      $options
    );

  }
}
